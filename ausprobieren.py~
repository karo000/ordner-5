import numpy as np
import random

A = np.array([[0,0],
              [2,1]])

B = np.array([5,2])

C = np.random.random(size=(2,3))
c = np.array([3, 10])
#b = np.random.random(size=(2,))

#A_inv = np.transpose(A)

print(3*B)
